(main (world ghost)
      (cons 99 (lambda (arg)
                 (main (state world)
 (let ((seed state)
       (mod (lambda (a m) (- a (* (/ a m) m))))
       )
   (let ((rng (lambda (_) (let ((next (mod (+ (* seed 5) 89219821) 1000000009)))
                            (begin (set! seed next) next))))
         (inc (lambda (x) (+ x 1)))
         (dec (lambda (x) (- x 1)))
         )
     (let ((nstate (rng 0)))
       (cons nstate (mod nstate 4)))
     )
   )
                       ))))
